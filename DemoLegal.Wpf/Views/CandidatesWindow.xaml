<Window x:Class="DemoLegal.Wpf.CandidatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Реестр кандидатов" Height="520" Width="960" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Реестр кандидатов (первые 500)" FontWeight="Bold" FontSize="16"/>
            <Button Content="Обновить" Margin="12,0,0,0" Padding="12,4" Click="Refresh_Click"/>
        </StackPanel>
        <DataGrid x:Name="GridCases" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column"
                  EnableRowVirtualization="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ЛС" Binding="{Binding Ls}" Width="120"/>
                <DataGridTextColumn Header="ФИО/Наименование" Binding="{Binding Fio}" Width="200"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="220"/>
                <DataGridTextColumn Header="Тип должника" Binding="{Binding DebtorType}" Width="100"/>
                <DataGridTextColumn Header="Сумма долга" Binding="{Binding DebtAmount}" Width="100" />
                <DataGridTextColumn Header="Период долга" Binding="{Binding Period}" Width="120"/>
                <DataGridTextColumn Header="Тип помещения" Binding="{Binding PremisesType}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
